import React from "react";
import { Button } from "../ui/button";

const LoginForm = () => {
    return (
        <div>
            <Button></Button>
        </div>
    );
};

export default LoginForm;
